{
  "private": true,
  "description": "Monorepo root – Vercel runs build from frontend/ via vercel-build",
  "dependencies": {
    "next": "16.1.6"
  },
  "scripts": {
    "vercel-build": "cd frontend && npm ci && npm run build && cp -r .next ../.next"
  }
}
