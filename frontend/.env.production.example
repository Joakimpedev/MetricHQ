# Vercel production env – set these in Vercel Project → Settings → Environment Variables.
# Use Production for deploy; optionally set same for Preview.

# Backend API URL (your Railway backend URL, e.g. https://profit-tracker-backend-production.up.railway.app)
NEXT_PUBLIC_API_URL=

# Clerk (use live keys for production)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
