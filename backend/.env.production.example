# Backend production env – set these in Railway (Variables).
# Copy to .env.production only for local production-like runs; do not commit real values.

PORT=4000
NODE_ENV=production

# Database (from Railway PostgreSQL service)
DATABASE_URL=

# Backend public URL (Railway service URL after deploy, e.g. https://xxx.up.railway.app)
BACKEND_URL=

# Frontend URL (Vercel app URL, e.g. https://profit-tracker.vercel.app)
FRONTEND_URL=

# Clerk (backend secret key)
CLERK_SECRET_KEY=

# TikTok Marketing API (optional)
TIKTOK_APP_ID=
TIKTOK_APP_SECRET=

# Meta Marketing API (optional)
META_APP_ID=
META_APP_SECRET=
