# Backend env vars – copy to .env locally; set in Railway for production.
# Do not commit .env (it is in .gitignore).

PORT=4000
NODE_ENV=development

# Database (Railway PostgreSQL – required in production)
DATABASE_URL=

# Backend public URL (required for OAuth callbacks – set in Railway)
BACKEND_URL=

# Frontend URL (required for CORS and OAuth redirects – set in Railway)
FRONTEND_URL=

# Clerk (validate JWT from frontend)
CLERK_SECRET_KEY=

# TikTok Marketing API (optional – for TikTok Ads OAuth)
TIKTOK_APP_ID=
TIKTOK_APP_SECRET=

# Meta Marketing API (optional – for Meta/Facebook Ads OAuth)
META_APP_ID=
META_APP_SECRET=
